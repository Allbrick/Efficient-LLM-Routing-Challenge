# 🚀 [프로젝트] "쉬운 문제는 싸게, 어려운 문제만 비싸게!" 가성비 100% AI 라우터 개발

본 프로젝트는 입력된 질문의 난이도와 유형을 실시간으로 파악하여, 쉬운 질문은 저렴하고 가벼운 모델에, 복잡하고 고난도의 질문만 고성능·고비용 모델에 지능적으로 배당하는 **'스마트 AI 배차 시스템(AI Router)'**을 개발하는 과제입니다. 한정된 예산 안에서 최소한의 비용으로 최적의 답변 품질을 이끌어내는 것을 목표로 합니다.

---

## 🎯 1. 개발 미션 및 배경

### 💡 기존 방식의 문제점
* `"안녕?"`이나 `"오늘 날씨 어때?"`와 같은 단순한 인사말이나 기초적인 질문에도 무조건 가장 비싸고 무거운 최고급 LLM(대형 언어 모델)을 호출하여 **불필요한 비용과 컴퓨팅 자원을 낭비**하는 구조였습니다.

### 🌟 새로운 목표 (비즈니스 임팩트)
* **예산 효율 극대화**: 한정된 예산(Budget) 범위 안에서 최소한의 비용으로 최고의 답변 품질을 도출합니다.
* **저예산 모드 최적화**: 특히 예산이 극도로 제한된 '저예산 티어(Tier)' 환경에서 효율적인 배차 능력을 증명하는 것이 이 프로젝트의 가장 핵심적인 평가 포인트입니다.

### 🏢 직관적 비유
> 단순 문의(예: 매장 영업시간 안내)는 **'일반 상담원(가볍고 저렴한 AI)'**에게 보내고, 복잡하고 전문적인 기술 지원이나 커스텀 코딩 요청만 **'전문 상담원(고성능·고비용 AI)'**으로 연결해 주는 **'스마트 고객센터 안내데스크'**를 구축하는 것과 같습니다. 운영비는 획기적으로 아끼면서, 고객이 체감하는 서비스 만족도(품질)는 최고 수준으로 유지합니다.

---

## 🛠️ 2. 기술 소개 및 챌린지 목표

* **저비용 모델 우선 활용**: 기본적으로 비용이 적게 드는 하위 모델을 우선적으로 검토하며, 고난도 문제로 판단될 때만 상위 모델로 승급(Escalation)하는 구조를 설계합니다.
* **불필요한 호출 최소화**: 다단계 호출 시 발생하는 중복 비용을 줄여 전체 파이프라인의 비용 효율성을 높입니다.
* **오픈소스 라우터 구현**: 외부 의존성 없이 재현 가능하며 완전히 공개할 수 있는 형태의 로컬 라우팅 알고리즘을 구현합니다.

### 💰 Budget Tier (예산 제한 조건)
평가는 다음 3가지 티어로 나뉘어 진행되며, **저예산 티어에 훨씬 높은 가중치**가 부여됩니다.
1. **Fast Tier** (극도의 저예산 / 빠른 응답 위주) ⭐⭐⭐ (최고 가중치)
2. **Balanced Tier** (비용과 품질의 균형) ⭐⭐
3. **Premium Tier** (넉넉한 예산 / 고품질 위주) ⭐

---

## 📋 3. 참가자 구현 내용

개발자는 주어진 입력 조건에 따라 어떤 후보 모델의 출력을 최종 선택할지 결정하는 **코드 기반 라우터(Code Router)**를 구현해야 합니다.

### 🔄 입출력 정의
* **입력(Input)**
    * 사용자의 입력 프롬프트 (Prompt)
    * 현재 타겟 예산 티어 (Budget Tier: Fast / Balanced / Premium)
    * 해당 요청 내의 이전 모델 호출 이력 (History)
    * 이용 가능한 후보 모델들의 메타데이터 및 비용 정보
* **출력(Output)**
    * `특정 후보 모델을 새롭게 호출`하는 액션 **또는**
    * `기존에 호출하여 얻은 출력 중에서 하나를 최종 답변으로 선택`하는 액션

### ⚠️ 중요 규칙 (제약 사항)
* ❌ **외부 API 및 네트워크 호출 절대 금지**: 모든 라우팅 로직은 로컬 코드로만 동작해야 하며, 외부 LLM API나 네트워크 서비스를 실시간으로 호출할 수 없습니다.
* 🎯 **최종 답안의 제한**: 최종적으로 선택하는 답변은 시스템이 정의한 후보 모델들의 출력 결과 중 하나여야 합니다. (라우터가 답변을 직접 생성하는 것이 아님)
* ⏱️ **지연 시간(Latency)**: Latency는 1차 점수(품질-비용)에는 반영되지 않으나, 점수가 완전히 동일할 경우 승패를 가르는 **Tie-break 기준**으로 활용됩니다.

---

## 🔄 4. 프로젝트 진행 방법

데이터셋은 **공개 데이터(Public)**와 **비공개 데이터(Private)** 두 가지 단계로 구성됩니다.

| 구분 | 제공 항목 및 특징 | 활용 목적 |
| :--- | :--- | :--- |
| **공개 데이터**<br>*(Public Set)* | • 프롬프트 및 도메인/태스크 정보<br>• 후보 모델별 실제 출력 결과 문장<br>• 각 출력 결과에 대한 품질 평가 정보 (Quality Score)<br>• 모델별 비용 및 상세 메타데이터 | 이 데이터를 기반으로 프롬프트 난이도를 분류하고, 비용 대비 품질을 예측하여 최적의 라우팅 정책(Policy)을 학습 및 설계합니다. |
| **비공개 데이터**<br>*(Private Set)* | • 프롬프트 정보만 제공<br>• 후보 출력 및 평가 정보는 숨겨짐<br>• **평가용 시뮬레이터** 형태로 작동 | 참가자가 작성한 라우터 코드가 시뮬레이터 안에서 동작합니다. 라우터가 선택한 `Action`에 대응하는 사전 계산 결과(비용 및 품질)를 바탕으로 최종 점수가 산정됩니다. |

---

## 📈 5. 평가 방식

라우터의 성능은 세 가지 Budget Tier 환경에서 시뮬레이션을 통해 종합적으로 측정됩니다.

1. **품질-비용 Trade-off 반영**: 주어진 예산 제약 조건(Budget Constraint)을 위반하지 않으면서 달성한 **평균 품질 점수**를 측정합니다.
2. **다중 Tier 가중치 산정**: 앞서 언급한 바와 같이, 예산이 쪼들리는 상황을 얼마나 잘 방어하는지 보기 위해 **Fast 및 Balanced 티어에 더 높은 가중치**를 부여하여 종합 점수를 계산합니다.
3. **동점자 처리 (Tie-break)**: 종합 점수가 완전히 일치할 경우, 라우팅 처리에 걸린 **지연 시간(Latency)이 짧은 팀**이 우승합니다.

---

## 💻 6. 개발 과제 예시 (접근 방법 및 아이디어)

본 프로젝트를 성공적으로 이끌기 위해 참가자는 다음과 같은 세부 과제를 연구 및 개발할 수 있습니다.

* **경량 Classifier 개발**: 입력된 프롬프트의 글자 수, 키워드, 도메인, 난이도 등을 CPU 환경에서도 수 밀리초(ms) 내에 판단할 수 있는 초경량 분류기(Classifier) 구축
* **Tier별 라우팅 전략 설계**: Fast 환경에서는 고비용 모델 호출을 원천 차단하는 하드 규칙(Hard Rule)을 적용하고, Premium 환경에서는 품질 중심의 Escalation 정책 설계
* **품질-비용 Trade-off 최적화 연구**: 강화학습(RL)의 밴딧(Bandit) 알고리즘이나 동적 계획법(DP) 등을 활용하여 비용당 기대 품질 점수를 극대화하는 수학적 모델 수립

---

## 📚 참고 자료

프로젝트 구현 및 inference-time 알고리즘 설계에 도움을 줄 수 있는 주요 연구 자료입니다.

* **NeurIPS 2024 Tutorial**: *Beyond Decoding: Meta-Generation Algorithms for LLMs*
* **Research Paper**: *From Decoding to Meta-Generation: Inference-time Algorithms for Large Language Models*
* **Empirical Analysis**: *Inference Scaling Laws: An Empirical Analysis of Compute-Optimal Inference*